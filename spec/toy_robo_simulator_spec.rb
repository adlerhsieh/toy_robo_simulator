require 'spec_helper'

describe ToyRoboSimulator do
  it 'has a version number' do
    expect(ToyRoboSimulator::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
